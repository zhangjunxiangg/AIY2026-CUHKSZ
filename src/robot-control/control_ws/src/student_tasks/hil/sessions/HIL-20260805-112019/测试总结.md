# HIL-20260805-112019 测试总结

## 结论

本轮完成底盘六方向原语、显式停车、控制权冲突、四向雷达净空和急停持久化/复位测试。控制输出、方向符号和停止链路现场均未发现异常；由于没有独立距离/角度量测与视频文件索引，六个方向 case 仍保持 `BLOCKED`，整体状态为 `HIL_PENDING`，没有提升为 `HIL_VERIFIED`。

## 方向原语

| Case | 请求 | CLI 非零发布 | Observer 非零 | 最后一帧 | 现场结果 |
|---|---|---:|---:|---|---|
| `DIR-XP` | `x=+0.10 m/s, 2.0 s` | 20 | 19 | 零速 | 前进正常，量测/视频待补 |
| `DIR-XN` | `x=-0.10 m/s, 2.0 s` | 19 | 18 | 零速 | 后退正常，量测/视频待补 |
| `DIR-YP` attempt 1 | `y=+0.10 m/s, 2.0 s` | 0 | 0 | 无消息 | 供电告警期间未执行，记 `BLOCKED` |
| `DIR-YP` attempt 2 | `y=+0.10 m/s, 2.0 s` | 17 | 16 | 零速 | 左移正常，量测/视频待补 |
| `DIR-YN` | `y=-0.10 m/s, 2.0 s` | 17 | 16 | 零速 | 右移正常，量测/视频待补 |
| `DIR-AZP` | `az=+0.15 rad/s, 2.0 s` | 17 | 15 | 零速 | 逆时针正常，量测/视频待补 |
| `DIR-AZN` | `az=-0.15 rad/s, 2.0 s` | 17 | 16 | 零速 | 顺时针正常，量测/视频待补 |

每个实际动作均单独展示参数与风险、获得一次明确的 `走` 后执行。每次完成后 CLI 报告 3 次零速尝试并确认成功，observer 最后一帧为零速，ROS graph 检查无残留 `/cmd_vel` publisher。

## 非运动测试

- `STOP-001`：正常状态显式停车通过，observer 仅收到零速。
- `STOP-002`：供电变化导致 `/scan` 不可用时，零速停车仍成功。
- `OWN-001`：第二个项目进程申请控制锁返回 `MOTION_BUSY`。
- `OWN-002`：不发布消息的未知 `/cmd_vel` publisher 被识别为 `CMD_VEL_PUBLISHER_CONFLICT`，退出后 graph 恢复干净。
- `SCAN-*-CLEAR`：前后左右四个方向均超过 `0.50 m` 阈值；场地无障碍，四个 blocked case 均为 `NOT_RUN`。
- `ESTOP-001`：测试锁存跨进程保持，新 CLI 读取到 `ESTOP_LATCHED`。
- `ESTOP-002`：停止雷达后复位返回 `BACKEND_UNAVAILABLE`，锁存和 0 个 clear frame 保持。
- `ESTOP-003`：雷达恢复后连续 3 帧四向净空完成复位，最终锁存为 false。

## 环境问题与交接

雷达 CH341 设备在供电和 USB 变化后经历 `/dev/ttyUSB1 → /dev/ttyUSB0 → /dev/ttyUSB1`。固定端口启动会出现节点占用失效端口、无 `/scan` 或直接退出。交给队友前最后一次按当前 `/dev/ttyUSB1` 手动启动，官方 `lidar-scan=PASS`，`scan_time=0.07256358861923218 s`。

该雷达进程不是生产自启动，后续必须先识别当前 CH341 端口。交接时已有队友进入板子调试，因此本文只记录交接前最后已知状态，不声明当前 PID、ROS graph 或 M-Claw 状态。

## 证据与未完成项

- 27 条 case JSON 全部通过 evidence schema：`16 PASS / 7 BLOCKED / 4 NOT_RUN / 0 FAIL`。
- 控制包完整离线回归：151 项通过。
- 原始 `/cmd_vel` observer 输出位于 `logs/`。
- 待补：六方向独立距离/角度、视频索引、四向受控障碍物测试、可恢复旧部署身份。
- 视觉能力保持 `approach=false`，不属于本轮底盘 HIL。
