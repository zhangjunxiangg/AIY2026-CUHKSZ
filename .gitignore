# 环境与缓存（不入库）
.venv/
_audit-cache/
.codegraph/
guizang-ppt-skill/
ppt/
node_modules/

# 个人任务系统（不共享）
.octie/

# PPT 工程（不入库）
0802-meeting-ppt/

# 大文件原件（>3MB，按需决定是否入库）
AIY黑客松x深开鸿赛题宣讲.pptx
AIY黑客松x深开鸿赛题宣讲.pdf
AIY黑客松x深开鸿赛题宣讲.log

# 研究论文与原始资料（体积大，本地参考）
papers/

# 部署临时扁平目录
prep/demo_flat/

# 每日复盘文档（本地参考，不入库）
docs/daily_review/

# 相机截图与运行产物
prep/demo记录/captures/
captures/
out/

# Office 临时文件
~$*.pptx
~$*.ppt
~$*.docx
~$*.doc

# SSH 密钥与证书（严禁入库）
*.pem
*.key
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*.ppk
*.pub
.ssh/
ssh-keys/
**/authorized_keys
**/known_hosts

# 环境变量与密钥配置
.env
.env.*
!.env.example
!.env.sample
!.env.template

# 外部源码树（本地参考，含独立 .git，不入库）
softbus-source/

# 深度滤波原始采集序列（体积大，可由 pipeline 脚本重新采集）
*.npy

# Python 缓存
__pycache__/
