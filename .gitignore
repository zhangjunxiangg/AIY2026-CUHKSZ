# 环境与缓存（不入库）
.venv/
_audit-cache/
.codegraph/
guizang-ppt-skill/
ppt/
node_modules/

# 个人任务系统（不共享）
.octie/

# PPT 工程（不入库）
0802-meeting-ppt/

# 大文件原件（>3MB，按需决定是否入库）
AIY黑客松x深开鸿赛题宣讲.pptx
AIY黑客松x深开鸿赛题宣讲.pdf
AIY黑客松x深开鸿赛题宣讲.log

# 个人/临时工具目录（避免误提交到根目录）
tools/

# 研究论文与原始资料（体积大，本地参考）
papers/

# 部署临时扁平目录
prep/demo_flat/

# 每日复盘文档（本地参考，不入库）
docs/daily_review/

# 相机截图与运行产物
prep/demo记录/captures/
captures/
out/

# Office 临时文件
~$*.pptx
~$*.ppt
~$*.docx
~$*.doc

# SSH 密钥与证书（严禁入库）
*.pem
*.key
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*.ppk
*.pub
.ssh/
ssh-keys/
**/authorized_keys
**/known_hosts

# 环境变量与密钥配置
.env
.env.*
!.env.example
!.env.sample
!.env.template

# 外部源码树（本地参考，含独立 .git，不入库）
softbus-source/

# 深度滤波原始采集序列（体积大，可由 pipeline 脚本重新采集）
*.npy

# Python 缓存
__pycache__/

# Gemini 运行产物（视频/采集数据，本地保留不入库，可由 pipeline 重新生成）
gemini335-web/depth-filter/run*/
gemini335-web/depth-filter/live*/
gemini335-web/snapshots/

# 板端克隆的驱动源码/导出包（本地参考，不入库）
astra-debug/

# 抓取/遥操运行产物与日志（本地保留）
grasping/logs/
teleop/captures/
car_output/
car_output.zip
perception.zip
practice.zip
yolo训练集1.0/
yolo训练集1.0.zip
car_output/
*.zip

# ===== 路演媒体大文件（2026-08-06，presentation 合入时补）=====
# 视频原件与成片单个动辄数百 MB，一律不入库；deck 里的视频是本地外挂播放，不依赖 git。
*.mp4
*.MP4
*.mov
*.MOV
presentation/素材/
**/.__jianying_export_temp_folder_*/
# 即梦生成的高清原图批次（过程素材，完整版留在 junxiang 分支）
presentation/assets/jimeng/

# ===== 队长个人卡片（私人内容，永不入库、永不推送）=====
docs/队长卡片/

# ===== Kimi 会话 prompt 存档（私人内容，永不入库、永不推送）=====
docs/kimi-prompts/

# ===== 隐私与个人内容（2026-08-09 全面豁免，永不入库、永不推送）=====
docs/exp-mclaw-kimi-coding-plan.md
mclaw-install/M-Claw安装artifacts记录.md
docs/队长个人卡片.md
docs/项目里程碑全程回顾.md
scripts/extract-kimi-prompts.py
scripts/cards-to-docx.sh
