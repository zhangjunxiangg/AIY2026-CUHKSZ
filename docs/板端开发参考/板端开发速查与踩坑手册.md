# 板端开发速查与踩坑手册

> 用途：汇总在 KaihongBoard-3588S 上验证过的数据结构与操作坑点，作为团队开发启动的统一参考。  
> 维护规则：新增内容标注日期与验证状态（【实测】/【推断】/【待确认】）。  
> 最近更新：2026-08-04

---

## 1. 板子连接与 HDC

### 1.1 HDC 模式切换

| 模式 | 命令 | 说明 |
|---|---|---|
| USB 模式 | `hdc -t <connect-key> tmode usb` | 拔插 USB 自动识别，比赛时默认 |
| TCP 模式 | `hdc -t <connect-key> tmode port 5555` | 配合 `hdc tconn <IP>:5555` 网络连接 |
| 查看模式 | `hdc list targets -v` | 可看到 USB / TCP / UART 状态 |

**踩坑**：执行 `tmode port 5555` 后 daemon 会保持 TCP 模式；若板子未连网或未插 USB，`tmode usb` 会失败（`Not match target founded`）。必须先让板子通过 USB 或网络被识别，才能切换模式。

**踩坑（2026-08-04 实测）**：`tmode port 5555` 后拔掉 USB 线，板子 HDC daemon 只监听 TCP；即使重新插 USB 线，Windows 设备管理器能看到 `"HDC Device"`（VID_2207&PID_5000），但 `Present: False`，`hdc list targets` 为空。**恢复方法**：
1. 让板子连上任意 WiFi（手机热点即可）；
2. `hdc tconn <板子IP>:5555` 网络连上；
3. `hdc -t <IP>:5555 tmode usb` 切回 USB 模式；
4. daemon 重启后 USB 自动识别。

### 1.2 USB 连接排障

1. 确认板子已通电。
2. 使用 **USB 公对公数据线**（非充电线），插到板子 **OTG 口**。
3. 电脑端更换 USB-A 口，必要时换线。
4. `hdc list targets -v` 查看是否有 USB 设备出现。
5. Windows 设备管理器查看 `VID_2207`（Rockchip）设备是否 `Present: True`；若 `Present: False` 说明 USB 物理层未枚举成功，不是 HDC 软件问题。

### 1.3 文件推送路径规则

**踩坑**：`hdc file send` 在此版本上目标路径**不能带前导 `/`**，否则会被解析到本地磁盘。正确写法：

```bash
hdc -t <target> file send local_file data/local/robot/
```

错误写法：

```bash
hdc -t <target> file send local_file /data/local/robot/   # 会失败
```

**踩坑（2026-08-04 实测）**：`hdc file send` 的目标路径若**不存在且带文件名**，会在板端创建一个**同名目录**而非文件。例如 `file send a.md data/local/robot/a.md` 且 `a.md` 不存在时，会创建 `/data/local/robot/a.md/` 目录。正确做法是先确保目录存在，再 `file send a.md data/local/robot/`。

**踩坑（2026-08-04 实测）**：`hdc file send` 会保留源文件的相对路径。若本地路径是 `docs/a.md`，目标是 `data/local/robot/`，板端会得到 `/data/local/robot/docs/a.md`。如需扁平化，先 `cd` 到文件所在目录或使用 `prep/deploy.sh` 的扁平临时目录方案。

### 1.4 一键部署脚本

`prep/deploy.sh` 已封装：先复制到扁平临时目录，再逐个 `hdc file send` 到 `data/local/robot/`，最后校验 YAML。用法：

```bash
bash prep/deploy.sh ec29004133314d38433031a523403c00
```

---

## 2. 运行时与环境变量

### 2.1 板端预装运行时

| 组件 | 路径 / 版本 | 验证状态 |
|---|---|---|
| Python | `/data/local/release/usr/bin/python3.12` | 【实测】 |
| Dora CLI | `dora-cli 0.3.12` | 【实测】 |
| ROS1 | 通过 `run` 包装器调用 | 【实测】 |
| M-Claw | 板端预装，API KEY 已配置 | 【已知】 |

### 2.2 Python 节点必须设置 LD_PRELOAD

**踩坑**：直接运行 Dora Python 节点会因缺少 `_Py_Dealloc` / `Py_DecRef` 符号崩溃。必须设置：

```bash
export LD_PRELOAD=/data/local/release/usr/lib/libpython3.12.so.1.0
```

在 Dora `dataflow.yml` 中每个 Python 节点都要加：

```yaml
env:
  LD_PRELOAD: /data/local/release/usr/lib/libpython3.12.so.1.0
  PYTHONUNBUFFERED: "1"
```

### 2.3 ROS1 使用 `run` 包装器

**踩坑**：不要直接用系统 `python3` 运行 ROS1 节点，会报 `libpython3.12.so` / `libintl.so` 找不到。正确方式：

```bash
run roscore &
run python3 talker.py &
run python3 listener.py
```

`run` 会自动设置 `PYTHONHOME`、`LD_LIBRARY_PATH`、`ROS_PACKAGE_PATH`。

---

## 3. Dora 数据流格式

### 3.1 节点输入事件

```python
{
    "type": "INPUT",              # 或 "STOP"
    "id": "dora/timer/millis/200",
    "value": pa.array([...]),     # pyarrow.Array
    "metadata": {"open_telemetry_context": b"..."}
}
```

定时器输入：
- `dora/timer/millis/200`：200ms tick，`value` 为 uint64 毫秒时间戳
- `dora/timer/millis/1000`：1s tick

### 3.2 节点输出 API

```python
node.send_output(
    "message",                  # dataflow.yml 中定义的 output id
    pa.array(["hello world"])   # 必须是 pyarrow.Array
)
```

### 3.3 常用命令

```bash
export LD_PRELOAD=/data/local/release/usr/lib/libpython3.12.so.1.0
export PATH=/data/local/release/usr/bin:/data/local/release/bin:$PATH
. /data/local/release/usr/setup.sh

dora up
dora start dataflow_hello.yml
dora list
dora check
dora stop <dataflow-uuid>
dora destroy
```

---

## 4. ROS1 话题与控制接口

### 4.1 传感器话题总览

| 传感器 | 话题 | 消息类型 | 频率 | 状态 |
|---|---|---|---|---|
| 思岚 A1 雷达 | `/scan` | `sensor_msgs/LaserScan` | ~13.7 Hz | 【实测】 |
| Astra RGB | `/astra_camera/rgb/image_raw` | `sensor_msgs/Image` | ~30 Hz | 【实测】 |
| Astra Depth | `/astra_camera/depth/image_raw` | `sensor_msgs/Image` (16UC1, mm) | ~30 Hz | 【实测】 |
| Astra RGB Info | `/astra_camera/rgb/camera_info` | `sensor_msgs/CameraInfo` | 与图像同发 | 【实测】 |
| Astra Depth Info | `/astra_camera/depth/camera_info` | `sensor_msgs/CameraInfo` | 与图像同发 | 【实测】 |
| 点云 | `/astra_camera/depth_registered/points` | `sensor_msgs/PointCloud2` | ~30 Hz | 【实测】 |
| 里程计 | `/odom` | `nav_msgs/Odometry` | ~30 Hz | 【实测】 |
| IMU | `/ros_robot_controller/imu_raw` | `sensor_msgs/Imu` | ~100 Hz | 【实测】 |
| 电池 | `/ros_robot_controller/battery` | `std_msgs/UInt16` | ~1 Hz | 【实测】 |
| 舵机状态 | `/servo_controllers/port_id_1/servo_states` | `servo_msgs/ServoStateList` | ~6 Hz | 【实测】 |
| 机械臂状态 | `/arm_controller/state` | `control_msgs/FollowJointTrajectoryFeedback` | 触发时 | 【实测】 |
| 夹爪状态 | `/gripper_controller/state` | `control_msgs/FollowJointTrajectoryFeedback` | 触发时 | 【实测】 |
| 底盘控制 | `/cmd_vel` | `geometry_msgs/Twist` | — | 【实测】 |

### 4.2 关键数据结构

#### `/scan`（LaserScan）

```yaml
header: { frame_id: "laser" }
angle_min: -3.1415927410125732
angle_max: 3.1415927410125732
angle_increment: 0.01745329238474369   # ~1°
range_min: 0.15000000596046448
range_max: 12.0
ranges: [float32 数组，约 360 点，inf 表示无返回]
intensities: [uint8 数组，有效返回约 13~15]
```

#### Astra 相机内参

```yaml
header: { frame_id: "astra_camera_color_optical_frame" }
height: 480
width: 640
distortion_model: "rational_polynomial"
K: [545.5231323242188, 0.0, 320.628173828125,
    0.0, 545.5231323242188, 239.10073852539062,
    0.0, 0.0, 1.0]
D: [0.11155129224061966, -0.18160298466682434, -0.0023391135036945343,
    0.0024356262292712927, -0.042575184255838394, 0.0, 0.0, 0.0]
```

**踩坑**：深度图 `D` 全为 0，当前驱动未启用深度畸变校正；RGB 有畸变。

#### `/odom`（Odometry）

```yaml
header: { frame_id: "odom" }
child_frame_id: "base_footprint"
pose: { position: {x,y,z}, orientation: {quaternion}, covariance: [...] }
twist: { linear: {x,y,z}, angular: {x,y,z}, covariance: [...] }
```

#### `/ros_robot_controller/imu_raw`（Imu）

```yaml
header: { frame_id: "imu_link" }
orientation: {x:0, y:0, z:0, w:0}   # 未融合
angular_velocity: {x: rad/s, y: rad/s, z: rad/s}
linear_acceleration: {x: m/s², y: m/s², z: m/s²}
```

#### `/ros_robot_controller/battery`（UInt16）

样本值 `12260`，推测为电压 mV（≈12.26V），量纲待确认。

#### `/servo_controllers/port_id_1/servo_states`（ServoStateList）

```yaml
servo_states:
  - {timestamp: 1609507137.488, id: 1, goal: 562, position: 562, error: 0, voltage: 9000}
  - {timestamp: 1609507137.509, id: 2, goal: 507, position: 507, error: 0, voltage: 9000}
  # ...
```

id 1-5 为 5 个机械臂关节舵机，id 10 为夹爪/旋转关节（待确认）。

#### `/cmd_vel`（Twist）

```yaml
linear: {x: m/s, y: m/s, z: 0}
angular: {x: 0, y: 0, z: rad/s}
```

安全限制（源码推断）：合速度 ≤ 0.20 m/s，角速度 ≤ 0.50 rad/s，单次时长 0.1–10.0 s。

#### FollowJointTrajectory Action

- 机械臂 Goal：`/arm_controller/follow_joint_trajectory/goal`
- 夹爪 Goal：`/gripper_controller/follow_joint_trajectory/goal`

```yaml
trajectory:
  joint_names: [joint1, joint2, joint3, joint4, joint5]   # 或 [r_joint]
  points:
    - positions: [rad...]
      velocities: [rad/s...]
      accelerations: [rad/s²...]
      time_from_start: {secs: 1, nsecs: 0}
```

---

## 5. M-Claw `robot_ops` JSON 命令

### 5.1 命令列表

| 命令 | 作用 | 参数示例 |
|---|---|---|
| `chassis-move` | 底盘按速度/时长运动 | `{"linear_x": 0.1, "angular_z": 0.0, "duration": 1.0}` |
| `chassis-stop` | 底盘停止 | `{}` |
| `arm-set-one` | 设置单个关节角度 | `{"joint": 1, "angle": 0.0, "duration": 1.0}` |
| `gripper-set` | 设置夹爪开合 | `{"position": 0.5, "duration": 1.0}` |
| `camera-capture` | 相机拍照 | `{}` |
| `lidar-scan` | 获取雷达单次扫描 | `{}` |
| `health` | 健康检查 | `{}` |
| `status` | 整机状态 | `{}` |
| `interfaces` | 列出可用接口 | `{}` |

### 5.2 响应格式

```json
{
  "ok": true,
  "command": "chassis-move",
  "result": { ... },
  "error": null
}
```

---

## 6. 板上运行时启动顺序

官方脚本位于 `/data/robot-host/`：

1. `start-host-chassis.sh`：roscore + 底盘控制 + 里程计
2. `start-lidar-4.1.sh`：RPLIDAR `/scan`
3. `start-vision-4.1.sh`：Astra 深度相机
4. `start-arm-4.1.sh`：机械臂与夹爪控制器
5. `healthcheck-all-4.1.sh`：全链路自检

**踩坑**：3 号舵机若读取不到状态，需将其向上调整至直立或略朝上（指导老师建议）。

**踩坑（2026-08-04 实测）**：`rk3588s-vision` 容器可能静默退出或相机节点僵死——表现为 `/astra_camera/*` 话题在 `rostopic list` 中可见但 `rostopic hz` 显示 `no new messages`（或容器已不在 `docker ps` 中）。修复：`sh /data/robot-host/start-vision-4.1.sh` 重启容器（已退出时），或 `docker restart rk3588s-vision`（容器在但不出数时），约 10s 后恢复 ~20-30Hz 出图。

**踩坑（2026-08-04 实测）**：`hdc file recv` 的板端源路径同样不能带前导 `/`（否则被解析到 PC 本地临时目录报 no such file），写 `tmp/captures/x.jpg` 而非 `/tmp/captures/x.jpg`。

---

## 7. 待确认清单

| 项 | 状态 |
|---|---|
| Gemini 335 辅助相机话题与接入方式 | 【待确认】 |
| 电池话题量纲（12260 = 12.26V？） | 【待确认】 |
| 舵机 id 10 的精确功能 | 【待确认】 |
| 触控屏写入接口 | 【待确认】 |
| 底盘急停/故障状态话题 | 【待确认】 |
| 现场比赛场地坐标系与定位需求 | 【待确认】 |
| 机械臂各关节安全工作范围数值 | 【待确认】 |

---

## 9. SSH 配置踩坑（2026-08-04 实测）

### 9.1 密码设置

- 板子无 `busybox chpasswd`，`echo "root:xxx" | chpasswd` 不可用。
- `passwd root` 需要 TTY，通过 HDC 管道输入会报 `tcsetattr pipe: Not a tty`。
- **可行方案**：先用 `run openssl passwd -1 '123456'` 生成 MD5 哈希，再写入 `/etc/passwd` 第二字段；或创建 `/etc/shadow` 写入哈希。

### 9.2 密码文件位置

- 该 OpenHarmony 系统默认**没有 `/etc/shadow`**，密码哈希直接放在 `/etc/passwd` 第二字段。
- 但部分 SSH 实现仍期望读 `/etc/shadow`；建议两个文件都写：
  - `/etc/passwd`：`root:<hash>:0:0::/data/robot-host:/bin/sh`
  - `/etc/shadow`：`root:<hash>:18000:0:99999:7:::`（权限 600）

### 9.3 SSH 认证失败排查

- 现象：`Permission denied (publickey,password,keyboard-interactive)`。
- 可能原因：哈希格式不被支持（MD5 `$1$` vs SHA-512 `$6$`）、sshd 走了 PAM 且 PAM 配置不识别该哈希、或密码文件权限不对。
- 备用方案：改用密钥认证，把客户端公钥写入 `/root/.ssh/authorized_keys`。

### 9.4 sshd 配置路径

- 模板：`/data/local/release/usr/etc/sshd_config`
- 生效：`/usr/etc/sshd_config`（需手动复制）
- 必须项：`PermitRootLogin yes`、`PasswordAuthentication yes`
- host key 生成：`run ssh-keygen -A`
- 启动前需创建：`/var/empty`、`/var/run`、`/var/log`

---

## 10. Windows 开发机脚本踩坑（2026-08-04 实测）

### 10.1 Git Bash 调用 PowerShell

- 在 Git Bash 中执行 `powershell -Command "... $_ ..."` 时，`$_` 会被 Bash 解释为变量，导致 PowerShell 脚本块解析失败。
- **解决**：用 `powershell -NoProfile -Command "... \$_ ..."` 转义，或把 PowerShell 脚本写成 `.ps1` 文件再调用。

### 10.2 Windows Batch 中文编码

- `.bat` 文件若含中文注释/输出，在部分 CMD 代码页下会解析成乱码并报 `'xxx' 不是内部或外部命令`。
- **解决**：改用 Bash 脚本（Git Bash）或 PowerShell 脚本，避免在 Batch 中使用中文。

### 10.3 Zadig 驱动工具

- Zadig 只能给**当前已连接**的 USB 设备安装驱动；若设备 `Present: False` 则列表里看不到。
- 若 HDC 设备未枚举，Zadig 无法修复，需先解决物理连接问题。

---

## 11. 开发启动检查清单

1. `hdc list targets -v` 确认板子在线（USB 或 TCP）。
2. 确认当前 HDC 模式（`hdc list targets`）。
3. 运行 `healthcheck-all-4.1.sh` 确认全链路。
4. Python 节点已设置 `LD_PRELOAD`。
5. ROS1 节点通过 `run` 启动。
6. 控制指令前确认机械臂在安全姿态，运动范围内无障碍。
7. 代码改动先提交 Git，再用 `prep/deploy.sh` 推送板端。
8. 如需 SSH，确认 `/etc/passwd` 与 `/etc/shadow` 均已写入密码哈希，且 sshd 在 22 端口监听。
